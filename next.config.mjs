/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "api.microlink.io", "cdn.prod.website-files.com", "cdn.jsdelivr.net", "upload.wikimedia.org"],
    }
};

export default nextConfig;
